---
title: TkxTransferList
description: TransferList — accessible React component from tekivex-ui. Variants, props, and accessibility notes.
---

```tsx
import { TkxTransferList } from 'tekivex-ui';
```

TransferList is part of the 78-component tekivex-ui library. Every component is
WCAG 2.1 AAA compliant, fully typed, and tree-shakeable.

## Props

| Prop | Type | Required | Description |
|---|---|---|---|
| `sourceItems` | `TransferItem[]` | yes |  |
| `targetItems` | `TransferItem[]` | yes |  |
| `onTransfer` | `(source: TransferItem[], target: TransferItem[]) => void` | yes |  |
| `sourceTitle` | `string` |  |  |
| `targetTitle` | `string` |  |  |
| `searchable` | `boolean` |  |  |
| `height` | `number` |  |  |
| `className` | `string` |  |  |
| `style` | `CSSProperties` |  |  |

## Accessibility

- Keyboard navigation supported on all interactive elements
- `:focus-visible` outline at 2px (theme-token `primary`)
- ARIA roles and properties applied per WAI-ARIA 1.2
- Respects `prefers-reduced-motion`
- Minimum 44×44 touch target where applicable

## Source

The implementation lives at [`src/components/TkxTransferList.tsx`](https://github.com/007krcs/tekivex-ui/blob/master/src/components/TkxTransferList.tsx).

> This page is a generated scaffold. It will be replaced with hand-authored
> examples and a live preview in a follow-up release. For interactive demos,
> see the [legacy demo site](https://ui.tekivex.com/#/components/transfer-list).
