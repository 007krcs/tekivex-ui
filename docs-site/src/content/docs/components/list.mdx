---
title: TkxList
description: Structured item list with avatars, descriptions, actions, status badges, and a loading state.
---

import { ListBasic, ListWithAvatars, ListWithExtra, ListLoading } from '../../../components/demos/ListDemo';

```tsx
import { TkxList } from 'tekivex-ui';
```

## Basic

<ListBasic client:load />

```tsx
<TkxList
  bordered
  items={[
    { key: '1', title: 'README.md', description: 'Last edited 2 hours ago' },
    { key: '2', title: 'CHANGELOG.md', description: 'Last edited yesterday' },
  ]}
/>
```

## With avatars + actions

<ListWithAvatars client:load />

## With extra metadata (badges)

<ListWithExtra client:load />

## Loading state

<ListLoading client:load />

## Props

| Prop | Type | Required | Description |
|---|---|---|---|
| `items` | `ListItem[]` | yes |  |
| `header` | `ReactNode` |  |  |
| `footer` | `ReactNode` |  |  |
| `bordered` | `boolean` |  |  |
| `size` | `'sm' \| 'md' \| 'lg'` |  |  |
| `loading` | `boolean` |  |  |
| `emptyText` | `string` |  |  |
| `grid` | `{ column?: number; gutter?: number }` |  |  |

## Accessibility

- Keyboard navigation supported on all interactive elements
- `:focus-visible` outline at 2px (theme-token `primary`)
- ARIA roles and properties applied per WAI-ARIA 1.2
- Respects `prefers-reduced-motion`
- Minimum 44×44 touch target where applicable

## Source

The implementation lives at [`src/components/TkxList.tsx`](https://ui.tekivex.com).

For the full interactive sandbox, open the [playground](/playground/#/components/list).
