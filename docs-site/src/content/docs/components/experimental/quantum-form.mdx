---
title: TkxQuantumForm
description: QuantumForm — experimental React component from tekivex-ui. API not stable.
---

import { Aside } from '@astrojs/starlight/components';

<Aside type="caution" title="Experimental — API not stable">
  This component lives in the `tekivex-ui/experimental` subpath. Its API may change or be removed
  between minor versions. Pin your version explicitly and check the [CHANGELOG](https://github.com/007krcs/tekivex-ui/blob/master/CHANGELOG.md) before upgrading.
</Aside>

```tsx
import { TkxQuantumForm } from 'tekivex-ui/experimental';
```

QuantumForm is part of the **experimental** surface of tekivex-ui. It is not
covered by the same WCAG 2.1 AAA self-test or test-coverage commitments as the
production components. Use it for prototypes; pin and audit before production.

## Props

| Prop | Type | Required | Description |
|---|---|---|---|
| `fields` | `QuantumFieldConfig[]` | yes |  |
| `onSubmit` | `(values: Record<string, string>) => void \| Promise<void>` | yes |  |
| `submitLabel` | `string` |  |  |
| `layout` | `'vertical' \| 'horizontal'` |  |  |
| `showConfidence` | `boolean` |  |  |
| `showQuantumState` | `boolean` |  |  |

## Accessibility

- Keyboard navigation supported on all interactive elements
- `:focus-visible` outline at 2px (theme-token `primary`)
- ARIA roles and properties applied per WAI-ARIA 1.2
- Respects `prefers-reduced-motion`
- Minimum 44×44 touch target where applicable

## Source

The implementation lives at [`src/components/TkxQuantumForm.tsx`](https://ui.tekivex.com).

> This page is a generated scaffold. It will be replaced with hand-authored
> examples and a live preview in a follow-up release. For interactive demos,
> see the [legacy demo site](https://ui.tekivex.com/#/components/quantum-form).
