---
title: TkxAIThinking
description: AIThinking — experimental React component from tekivex-ui. API not stable.
---

import { Aside } from '@astrojs/starlight/components';

<Aside type="caution" title="Experimental — API not stable">
  This component lives in the `tekivex-ui/experimental` subpath. Its API may change or be removed
  between minor versions. Pin your version explicitly and check the [CHANGELOG](https://github.com/007krcs/tekivex-ui/blob/master/CHANGELOG.md) before upgrading.
</Aside>

```tsx
import { TkxAIThinking } from 'tekivex-ui/experimental';
```

AIThinking is part of the **experimental** surface of tekivex-ui. Use it for
prototypes; pin and audit before production.

## Props

| Prop | Type | Required | Description |
|---|---|---|---|
| `label` | `string` |  | Text shown while thinking |
| `steps` | `string[]` |  | What steps the AI is working through (optional animated list) |
| `size` | `'sm' \| 'md' \| 'lg'` |  | Size variant |
| `variant` | `'dots' \| 'pulse' \| 'wave' \| 'orbit'` |  | Visual style |
| `active` | `boolean` |  | Whether actively thinking (false = done) |
| `className` | `string` |  |  |
| `style` | `CSSProperties` |  |  |

## Accessibility

- Keyboard navigation supported on all interactive elements
- `:focus-visible` outline at 2px (theme-token `primary`)
- ARIA roles and properties applied per WAI-ARIA 1.2
- Respects `prefers-reduced-motion`
- Minimum 44×44 touch target where applicable

## Source

The implementation lives at [`src/components/TkxAIThinking.tsx`](https://www.tekivex.com/ui).

> This page is a generated scaffold. It will be replaced with hand-authored
> examples and a live preview in a follow-up release. For interactive demos,
> see the [legacy demo site](https://www.tekivex.com/ui/#/components/aithinking).
