---
title: TkxMarkdown
description: Markdown — accessible React component from tekivex-ui. Variants, props, and accessibility notes.
---

```tsx
import { TkxMarkdown } from 'tekivex-ui';
```

Markdown is part of the 78-component tekivex-ui library. Every component is
WCAG 2.1 AAA compliant, fully typed, and tree-shakeable.

## Props

| Prop | Type | Required | Description |
|---|---|---|---|
| `source` | `string` |  | Markdown source string. Takes precedence over `src`. |
| `src` | `string` |  | Remote URL (or relative path) to a .md file. Fetched on mount. |
| `maxWidth` | `number \| string` |  | Max content width. Default: '100%'. |
| `compact` | `boolean` |  | Render in a visually compact variant (smaller headings/line-heights). |
| `copyableCode` | `boolean` |  | Show a copy button on fenced code blocks. Default: true. |
| `onLinkClick` | `(href: string, ev: React.MouseEvent<HTMLAnchorElement>) => boolean \| void` |  | Called when a link is clicked. Return false to suppress navigation. |
| `externalLinkRel` | `string` |  | Custom rel= value for external links. Default: 'noopener noreferrer'. |
| `externalLinkTarget` | `'_blank' \| '_self' \| '_parent' \| '_top'` |  | target= for external links. Default: '_blank'. |
| `loadingFallback` | `ReactNode` |  | Replace the loading / error fallback. |
| `errorFallback` | `(error: string) => ReactNode` |  |  |
| `className` | `string` |  |  |
| `style` | `CSSProperties` |  |  |

## Accessibility

- Keyboard navigation supported on all interactive elements
- `:focus-visible` outline at 2px (theme-token `primary`)
- ARIA roles and properties applied per WAI-ARIA 1.2
- Respects `prefers-reduced-motion`
- Minimum 44×44 touch target where applicable

## Source

The implementation lives at [`src/components/TkxMarkdown.tsx`](https://github.com/007krcs/tekivex-ui/blob/master/src/components/TkxMarkdown.tsx).

> This page is a generated scaffold. It will be replaced with hand-authored
> examples and a live preview in a follow-up release. For interactive demos,
> see the [legacy demo site](https://ui.tekivex.com/#/components/markdown).
