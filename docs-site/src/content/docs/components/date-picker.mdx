---
title: TkxDatePicker
description: DatePicker — accessible React component from tekivex-ui. Variants, props, and accessibility notes.
---

```tsx
import { TkxDatePicker } from 'tekivex-ui';
```

DatePicker is part of the 78-component tekivex-ui library. Every component is
WCAG 2.1 AAA compliant, fully typed, and tree-shakeable.

## Props

| Prop | Type | Required | Description |
|---|---|---|---|
| `value` | `Date \| null` |  | Value |
| `onChange` | `(date: Date \| null) => void` |  |  |
| `rangeValue` | `[Date \| null, Date \| null]` |  |  |
| `onRangeChange` | `(range: [Date \| null, Date \| null]) => void` |  |  |
| `multiValue` | `Date[]` |  |  |
| `onMultiChange` | `(dates: Date[]) => void` |  |  |
| `mode` | `DatePickerMode` |  | Config |
| `minDate` | `Date` |  |  |
| `maxDate` | `Date` |  |  |
| `disabledDates` | `Date[] \| ((date: Date) => boolean)` |  |  |
| `locale` | `string` |  |  |
| `dateFormat` | `string` |  |  |
| `showTime` | `boolean` |  | Time picker |
| `timeValue` | `{ h: number; m: number }` |  |  |
| `onTimeChange` | `(time: { h: number; m: number }) => void` |  |  |
| `showPresets` | `boolean` |  | Presets |
| `customPresets` | `DatePreset[]` |  |  |
| `label` | `string` |  | UI |
| `placeholder` | `string` |  |  |
| `hint` | `string` |  |  |
| `isDisabled` | `boolean` |  |  |
| `isInvalid` | `boolean` |  |  |
| `errorMessage` | `string` |  |  |
| `numberOfMonths` | `number` |  |  |
| `id` | `string` |  |  |
| `className` | `string` |  |  |
| `style` | `CSSProperties` |  |  |

## Accessibility

- Keyboard navigation supported on all interactive elements
- `:focus-visible` outline at 2px (theme-token `primary`)
- ARIA roles and properties applied per WAI-ARIA 1.2
- Respects `prefers-reduced-motion`
- Minimum 44×44 touch target where applicable

## Source

The implementation lives at [`src/components/TkxDatePicker.tsx`](https://github.com/007krcs/tekivex-ui/blob/master/src/components/TkxDatePicker.tsx).

> This page is a generated scaffold. It will be replaced with hand-authored
> examples and a live preview in a follow-up release. For interactive demos,
> see the [legacy demo site](https://ui.tekivex.com/#/components/date-picker).
