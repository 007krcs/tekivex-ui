---
title: TkxIcon
description: Icon — accessible React component from tekivex-ui. Variants, props, and accessibility notes.
---

```tsx
import { TkxIcon } from 'tekivex-ui';
```

Icon is part of the 78-component tekivex-ui library. Every component is
WCAG 2.1 AAA compliant, fully typed, and tree-shakeable.

## Props

| Prop | Type | Required | Description |
|---|---|---|---|
| `name` | `IconName` |  |  |
| `size` | `number \| string` |  |  |
| `color` | `string` |  |  |
| `strokeWidth` | `number` |  |  |
| `filled` | `boolean` |  |  |
| `label` | `string` |  |  |
| `className` | `string` |  |  |
| `style` | `React.CSSProperties` |  |  |
| `children` | `ReactNode` |  |  |

## Accessibility

- Keyboard navigation supported on all interactive elements
- `:focus-visible` outline at 2px (theme-token `primary`)
- ARIA roles and properties applied per WAI-ARIA 1.2
- Respects `prefers-reduced-motion`
- Minimum 44×44 touch target where applicable

## Source

The implementation lives at [`src/components/TkxIcon.tsx`](https://github.com/007krcs/tekivex-ui/blob/master/src/components/TkxIcon.tsx).

> This page is a generated scaffold. It will be replaced with hand-authored
> examples and a live preview in a follow-up release. For interactive demos,
> see the [legacy demo site](https://ui.tekivex.com/#/components/icon).
