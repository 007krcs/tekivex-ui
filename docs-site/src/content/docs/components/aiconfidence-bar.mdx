---
title: TkxAIConfidenceBar
description: AIConfidenceBar — accessible React component from tekivex-ui. Variants, props, and accessibility notes.
---

```tsx
import { TkxAIConfidenceBar } from 'tekivex-ui';
```

AIConfidenceBar is part of the 78-component tekivex-ui library. Every component is
WCAG 2.1 AAA compliant, fully typed, and tree-shakeable.

## Props

| Prop | Type | Required | Description |
|---|---|---|---|
| `value` | `number` | yes | 0–100 confidence value |
| `label` | `string` |  | Optional label for the metric being shown |
| `showLabel` | `boolean` |  | Show numeric percentage text |
| `size` | `'sm' \| 'md' \| 'lg'` |  | Size of the bar |
| `animate` | `boolean` |  | Animate on mount |
| `className` | `string` |  |  |
| `style` | `CSSProperties` |  |  |

## Accessibility

- Keyboard navigation supported on all interactive elements
- `:focus-visible` outline at 2px (theme-token `primary`)
- ARIA roles and properties applied per WAI-ARIA 1.2
- Respects `prefers-reduced-motion`
- Minimum 44×44 touch target where applicable

## Source

The implementation lives at [`src/components/TkxAIConfidenceBar.tsx`](https://github.com/007krcs/tekivex-ui/blob/master/src/components/TkxAIConfidenceBar.tsx).

> This page is a generated scaffold. It will be replaced with hand-authored
> examples and a live preview in a follow-up release. For interactive demos,
> see the [legacy demo site](https://ui.tekivex.com/#/components/aiconfidence-bar).
