---
title: TkxWatermark
description: Watermark — accessible React component from tekivex-ui. Variants, props, and accessibility notes.
---

```tsx
import { TkxWatermark } from 'tekivex-ui';
```

Watermark is part of the tekivex-ui library. Every component is self-tested
against WCAG 2.1 AAA criteria, fully typed, and tree-shakeable. A third-party
AAA audit is on the roadmap, not completed.

## Props

| Prop | Type | Required | Description |
|---|---|---|---|
| `text` | `string \| string[]` | yes |  |
| `children` | `ReactNode` | yes |  |
| `rotate` | `number` |  |  |
| `gap` | `[number, number]` |  |  |
| `fontSize` | `number` |  |  |
| `color` | `string` |  |  |
| `zIndex` | `number` |  |  |
| `pattern` | `WatermarkPattern` |  | Layout mode. Defaults to 'tiled' (v2.6 behaviour). |
| `dynamic` | `boolean` |  | Re-render the watermark whenever the tab becomes visible again. |
| `refreshMs` | `number` |  | Periodic re-render interval in ms. 0 disables. |
| `intensifyOnDevtools` | `boolean` |  | When DevTools open, increase opacity and blur the underlying content. |
| `fingerprintId` | `string` |  | Optional fingerprint id (e.g. user id / session id). Hashed into the watermark. |

## Accessibility

- Keyboard navigation supported on all interactive elements
- `:focus-visible` outline at 2px (theme-token `primary`)
- ARIA roles and properties applied per WAI-ARIA 1.2
- Respects `prefers-reduced-motion`
- Minimum 44×44 touch target where applicable

## Source

The implementation lives at [`src/components/TkxWatermark.tsx`](https://ui.tekivex.com).

> This page is a generated scaffold. It will be replaced with hand-authored
> examples and a live preview in a follow-up release. For interactive demos,
> see the [legacy demo site](https://ui.tekivex.com/#/components/watermark).
