---
title: TkxAnchor
description: Anchor — accessible React component from tekivex-ui. Variants, props, and accessibility notes.
---

```tsx
import { TkxAnchor } from 'tekivex-ui';
```

Anchor is part of the tekivex-ui library. Every component is self-tested
against WCAG 2.1 AAA criteria, fully typed, and tree-shakeable. A third-party
AAA audit is on the roadmap, not completed.

## Props

| Prop | Type | Required | Description |
|---|---|---|---|
| `items` | `AnchorLink[]` | yes |  |
| `offsetTop` | `number` |  |  |
| `getCurrentAnchor` | `(activeLink: string) => string` |  |  |
| `onChange` | `(currentLink: string) => void` |  |  |

## Accessibility

- Keyboard navigation supported on all interactive elements
- `:focus-visible` outline at 2px (theme-token `primary`)
- ARIA roles and properties applied per WAI-ARIA 1.2
- Respects `prefers-reduced-motion`
- Minimum 44×44 touch target where applicable

## Source

The implementation lives at [`src/components/TkxAnchor.tsx`](https://ui.tekivex.com).

> This page is a generated scaffold. It will be replaced with hand-authored
> examples and a live preview in a follow-up release. For interactive demos,
> see the [legacy demo site](https://ui.tekivex.com/#/components/anchor).
