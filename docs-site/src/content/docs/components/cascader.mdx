---
title: TkxCascader
description: Cascader — accessible React component from tekivex-ui. Variants, props, and accessibility notes.
---

```tsx
import { TkxCascader } from 'tekivex-ui';
```

Cascader is part of the 78-component tekivex-ui library. Every component is
WCAG 2.1 AAA compliant, fully typed, and tree-shakeable.

## Props

| Prop | Type | Required | Description |
|---|---|---|---|
| `options` | `CascaderOption[]` | yes |  |
| `value` | `string[]` |  |  |
| `onChange` | `(value: string[], selectedOptions: CascaderOption[]) => void` |  |  |
| `placeholder` | `string` |  |  |
| `label` | `string` |  |  |
| `multiple` | `boolean` |  |  |

## Accessibility

- Keyboard navigation supported on all interactive elements
- `:focus-visible` outline at 2px (theme-token `primary`)
- ARIA roles and properties applied per WAI-ARIA 1.2
- Respects `prefers-reduced-motion`
- Minimum 44×44 touch target where applicable

## Source

The implementation lives at [`src/components/TkxCascader.tsx`](https://ui.tekivex.com).

> This page is a generated scaffold. It will be replaced with hand-authored
> examples and a live preview in a follow-up release. For interactive demos,
> see the [legacy demo site](https://ui.tekivex.com/#/components/cascader).
