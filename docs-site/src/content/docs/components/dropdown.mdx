---
title: TkxDropdown
description: Dropdown — accessible React component from tekivex-ui. Variants, props, and accessibility notes.
---

```tsx
import { TkxDropdown } from 'tekivex-ui';
```

Dropdown is part of the 78-component tekivex-ui library. Every component is
WCAG 2.1 AAA compliant, fully typed, and tree-shakeable.

## Props

| Prop | Type | Required | Description |
|---|---|---|---|
| `trigger` | `ReactNode` | yes |  |
| `items` | `DropdownItem[]` |  |  |
| `groups` | `DropdownGroup[]` |  |  |
| `placement` | `DropdownPlacement` |  |  |
| `onSelect` | `(key: string, item: DropdownItem) => void` |  |  |
| `selectedKeys` | `string[]` |  |  |
| `multiSelect` | `boolean` |  |  |
| `searchable` | `boolean` |  |  |
| `searchPlaceholder` | `string` |  |  |
| `closeOnSelect` | `boolean` |  |  |
| `disabled` | `boolean` |  |  |
| `maxHeight` | `number` |  |  |
| `minWidth` | `number` |  |  |
| `open` | `boolean` |  |  |
| `onOpenChange` | `(open: boolean) => void` |  |  |
| `portal` | `boolean` |  |  |
| `offset` | `number` |  |  |
| `renderItem` | `(item: DropdownItem, selected: boolean) => ReactNode` |  |  |

## Accessibility

- Keyboard navigation supported on all interactive elements
- `:focus-visible` outline at 2px (theme-token `primary`)
- ARIA roles and properties applied per WAI-ARIA 1.2
- Respects `prefers-reduced-motion`
- Minimum 44×44 touch target where applicable

## Source

The implementation lives at [`src/components/TkxDropdown.tsx`](https://github.com/007krcs/tekivex-ui/blob/master/src/components/TkxDropdown.tsx).

> This page is a generated scaffold. It will be replaced with hand-authored
> examples and a live preview in a follow-up release. For interactive demos,
> see the [legacy demo site](https://ui.tekivex.com/#/components/dropdown).
