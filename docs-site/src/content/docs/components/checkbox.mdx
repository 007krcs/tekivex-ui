---
title: TkxCheckbox
description: Accessible checkbox with controlled state, indeterminate, color schemes, error state, and full keyboard support.
---

import { CheckboxBasic, CheckboxStates, CheckboxColorSchemes, CheckboxWithError } from '../../../components/demos/CheckboxDemo';

```tsx
import { TkxCheckbox } from 'tekivex-ui';
```

## Basic

<CheckboxBasic client:load />

```tsx
const [checked, setChecked] = useState(true);
<TkxCheckbox checked={checked} onChange={setChecked} label="I agree to the terms" />
```

## States

<CheckboxStates client:load />

```tsx
<TkxCheckbox checked={true} onChange={...} label="Checked" />
<TkxCheckbox checked={false} onChange={...} label="Unchecked" />
<TkxCheckbox isIndeterminate label="Indeterminate" />
<TkxCheckbox checked={true} disabled label="Disabled (checked)" />
```

## Color schemes

<CheckboxColorSchemes client:load />

## With error message

<CheckboxWithError client:load />

## Props

| Prop | Type | Required | Description |
|---|---|---|---|
| `label` | `ReactNode` |  |  |
| `hint` | `string` |  |  |
| `isInvalid` | `boolean` |  |  |
| `errorMessage` | `string` |  |  |
| `isIndeterminate` | `boolean` |  |  |
| `size` | `CheckboxSize` |  |  |
| `colorScheme` | `'primary' \| 'success' \| 'danger' \| 'warning'` |  |  |

## Accessibility

- Keyboard navigation supported on all interactive elements
- `:focus-visible` outline at 2px (theme-token `primary`)
- ARIA roles and properties applied per WAI-ARIA 1.2
- Respects `prefers-reduced-motion`
- Minimum 44×44 touch target where applicable

## Source

The implementation lives at [`src/components/TkxCheckbox.tsx`](https://ui.tekivex.com).

For a full interactive sandbox where you can change every prop and watch
the rendered checkbox update in real time, open the
[playground](/playground/#/components/checkbox).
