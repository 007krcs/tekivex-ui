---
title: TkxSlider
description: Slider — accessible React component from tekivex-ui. Variants, props, and accessibility notes.
---

```tsx
import { TkxSlider } from 'tekivex-ui';
```

Slider is part of the 78-component tekivex-ui library. Every component is
WCAG 2.1 AAA compliant, fully typed, and tree-shakeable.

## Props

| Prop | Type | Required | Description |
|---|---|---|---|
| `value` | `number` |  |  |
| `defaultValue` | `number` |  |  |
| `onChange` | `(value: number) => void` |  |  |
| `rangeValue` | `[number, number]` |  |  |
| `onRangeChange` | `(range: [number, number]) => void` |  |  |
| `min` | `number` |  |  |
| `max` | `number` |  |  |
| `step` | `number` |  |  |
| `isRange` | `boolean` |  |  |
| `label` | `string` |  |  |
| `showValue` | `boolean` |  |  |
| `showTicks` | `boolean` |  |  |
| `tickCount` | `number` |  |  |
| `isDisabled` | `boolean` |  |  |
| `colorScheme` | `'primary' \| 'success' \| 'danger' \| 'warning'` |  |  |
| `size` | `'sm' \| 'md' \| 'lg'` |  |  |
| `marks` | `{ value: number; label: string }[]` |  |  |
| `orientation` | `'horizontal' \| 'vertical'` |  |  |
| `showTooltip` | `boolean \| 'hover' \| 'always'` |  |  |
| `formatValue` | `(value: number) => string` |  |  |
| `onChangeEnd` | `(value: number) => void` |  |  |
| `onRangeChangeEnd` | `(range: [number, number]) => void` |  |  |
| `gradient` | `boolean` |  |  |

## Accessibility

- Keyboard navigation supported on all interactive elements
- `:focus-visible` outline at 2px (theme-token `primary`)
- ARIA roles and properties applied per WAI-ARIA 1.2
- Respects `prefers-reduced-motion`
- Minimum 44×44 touch target where applicable

## Source

The implementation lives at [`src/components/TkxSlider.tsx`](https://github.com/007krcs/tekivex-ui/blob/master/src/components/TkxSlider.tsx).

> This page is a generated scaffold. It will be replaced with hand-authored
> examples and a live preview in a follow-up release. For interactive demos,
> see the [legacy demo site](https://ui.tekivex.com/#/components/slider).
